# Frontend

Recommended setup:

```bash
npx create-next-app@latest frontend --typescript --tailwind --eslint --app
```

Core frontend responsibilities:

- PDF upload
- PDF.js drawing viewer
- SVG/Canvas overlays
- Object side panel
- Analysis result panel
- Isolation proposal view

Do not put AI logic in the frontend. The frontend only displays and corrects engineering intelligence generated by the backend.
